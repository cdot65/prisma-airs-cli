# Type Alias: ConsumptionTimeInterval

> **ConsumptionTimeInterval** = `7` \| `30` \| `60`

Defined in: src/airs/types.ts:1177

Allowed values for `--time-interval`. The API enforces this enum (other values return 400).

---
"@cdot65/prisma-airs-cli": minor
---

Upgrade SDK to v0.7.1, fix target create/restore 422 errors. Backup field names now use `target_background`/`target_metadata` (legacy names auto-normalized on restore). Remove WEBSOCKET provider. Scaffold targets default to `APPLICATION` type.

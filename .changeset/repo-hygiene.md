---
"@cdot65/prisma-airs-cli": patch
---

Repo and docs hygiene: trim the README down to install + quick start (full detail now lives on the documentation site), remove obsolete files and the unused autogenerated CLI-docs tooling, relocate `biome.json` to `config/` and the `Dockerfile` to `docker/`, and drop the root `CHANGELOG.md` in favor of the documentation site's release notes + GitHub Releases.

---
"@cdot65/prisma-airs-cli": patch
---

Redact AI Gateway API-key material from service and user list/detail output by default. Use the
explicit `--reveal-sensitive` flag when the stored key value is intentionally required.

---
"@cdot65/prisma-airs-cli": minor
---

Add `airs runtime topics get <nameOrId>` command to retrieve a custom topic by name or UUID. Displays name, description, examples, and metadata. Supports `--output pretty|json|yaml`.

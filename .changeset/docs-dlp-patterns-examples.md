---
"@cdot65/prisma-airs-cli": patch
---

docs(dlp/patterns): add live input/output examples for `runtime dlp patterns` (`list`, `create`, `get`, `replace`, `patch`, `delete`). `get`/`replace`/`patch` carry `!!! bug` admonitions linking to the upstream-400 tracking issues (#80, #98) with workarounds.
